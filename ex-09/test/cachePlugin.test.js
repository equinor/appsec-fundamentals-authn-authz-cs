'use strict';

const { test } = require('tap');

test('Cache pluging will not be tested', async (t) => {
    t.test('Success', (t) => {
       
        t.pass('Success');


        t.end();
    });

    t.end();
});
